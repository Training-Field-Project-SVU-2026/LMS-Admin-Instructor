import 'package:flutter/widgets.dart';

class CoursesInstructorMobileScreen extends StatelessWidget {
  const CoursesInstructorMobileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Courses Mobile View (Coming Soon)'),
    );
  }
}
